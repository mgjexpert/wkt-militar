# App
NEXT_PUBLIC_APP_URL=https://treinomilitar.novidades.store
AUTH_SECRET=replace-with-a-long-random-secret
DEMO_ACCESS_CODE=replace-for-preview-only

# Database (Neon/Postgres via Vercel Marketplace)
DATABASE_URL=

# Product
WKT_PRICE_CENTS=6700

# XPayments PIX S2S
XPAYMENTS_MODE=mock
XPAYMENTS_API_KEY=
XPAYMENTS_CHARGE_URL=https://api.xpayments.digital/api/v1/payments/charge

# Webhook endpoint to configure in XPayments:
# https://treinomilitar.novidades.store/api/webhooks/xpayments
#
# Do not invent a webhook secret. Add one only if XPayments supplies
# a signing secret/signature scheme in its dashboard/documentation.
